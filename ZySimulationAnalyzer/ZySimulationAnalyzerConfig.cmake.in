# CMake package configuration file for the ZyklioSimulationAnalyzer library

@PACKAGE_INIT@

if(NOT TARGET ZyklioSimulationAnalyzer)
    include("${CMAKE_CURRENT_LIST_DIR}/TruVelocityApproximationTargets.cmake")
endif()

check_required_components(ZyklioSimulationAnalyzer)
