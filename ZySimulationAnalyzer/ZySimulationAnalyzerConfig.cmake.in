# CMake package configuration file for the TruPhysicsSimulationAnalyzer library

@PACKAGE_INIT@

if(NOT TARGET TruPhysicsSimulationAnalyzer)
    include("${CMAKE_CURRENT_LIST_DIR}/TruVelocityApproximationTargets.cmake")
endif()

check_required_components(TruPhysicsSimulationAnalyzer)
