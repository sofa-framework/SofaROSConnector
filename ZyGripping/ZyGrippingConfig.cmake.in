# CMake package configuration file for the ZyGripping library

@PACKAGE_INIT@

if(NOT TARGET ZyGripping)
    include("${CMAKE_CURRENT_LIST_DIR}/ZyGrippingTargets.cmake")
endif()

check_required_components(ZyGripping)
